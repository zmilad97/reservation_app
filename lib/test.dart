
import 'package:flutter/material.dart';

class Milad extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('HI all'
      ,style: TextStyle(
          fontSize: 50,
        ),)),
    );
  }
}
