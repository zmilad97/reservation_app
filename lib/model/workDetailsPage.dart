import 'package:flutter/material.dart';

class WorkDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Text('this is Work Details Page '),
      ),
    );
  }
}
